# 05 简历可写性分级

> 编号说明：本目录另有《05-实现规格.md》，两文件同前缀共存（应任务指定命名，
> 不改）。分级图例：✅ 已实现/已有数据，可以直接写与说；🟡 机制/方案已就绪，
> **数字待跑**，写之前必须把数字填上；❌ 无代码依据，写了就是造假。

## 1. 评测相关声明的可写性总表

| 声明 | 分级 | 依据与备注 |
| --- | --- | --- |
| "基于 GitBugs 10.6 万条 issue 自建评测集" | ✅ | 106,714 条快照、清洗后 106,656；golden set 8,599 条有效 query（build_stats 落盘） |
| "评测集三处失效修正（可达性/自命中/family 划分）" | ✅ | v2 已实现有测试；34%、13104→8599、3,333 family 全部可复现 |
| "nDCG IDCG 修复" | ✅ | v2 metrics.py + 回归测试 |
| "随机性控制：检索单次评估 + LLM t=0 重复 3 次" | ✅（方案）/🟡（LLM 侧数字） | 00/04 文档；检索层已按此跑；LLM 侧等充值 |
| "向量基线 Recall@10 / nDCG@10" | ✅ | 已实测：0.3965 / 0.2886（200 条抽样，自命中剔除） |
| "混合+精排终点指标" | 🟡 | run_eval 代码就绪，hybrid/hybrid_rerank 待跑 |
| **"粒度不对称单独贡献 +__pp"（新消融组①）** | 🟡 | 03 文档主链②③设计完毕；需先实现 chunk 粒度 BM25 索引（05-实现规格 suite/chunk_bm25.py），再跑 hyb_sym vs hyb_asym |
| **"重试机制净收益（修正−恶化）"（新消融组②）** | 🟡 | 03 文档第 3 节设计完毕（触发率/命中率/token 代价/逐条三分类/阈值扫描/轮次边际）；依赖 LLM 可用 |
| **"hard 负样本上两阶段精排误判率下降"（新消融组③）** | 🟡 | 02 文档 C2/C3 设计完毕（挖掘流程+对照口径）；检索层确定性跑一次即可，不依赖 LLM |
| "Recall@10 0.61→0.82 (+21pp) 等旧数字" | ❌ | 任何版本代码与结果文件中不存在（详见 指导/00-版本差异.md 第 5 节），必须弃用 |

## 2. 当前简历措辞对照

### 2.1 简历原文（当前版）

> 评估与消融：基于 GitBugs 10.6 万条 issue 自建评测集，逐组件消融拆解
> 净贡献：向量基线 → +BM25 RRF 融合 → +粒度不对称 → +Cross-Encoder
> 精排，Recall@10 \_\_→\_\_、nDCG@10 \_\_→\_\_，其中粒度不对称单独贡献 +\_\_pp。

### 2.2 逐分句依据

| 分句 | 分级 | 依据文件 | 关键代码/事实 |
| --- | --- | --- | --- |
| "基于 GitBugs 10.6 万条 issue 自建评测集" | ✅ | eval/golden_set_builder.py；评测/01 | 可达性过滤核心（内嵌如下） |
| "逐组件消融拆解净贡献" | 🟡 | 评测/03 主链①②③④ | 链设计完毕；③②对照即"净贡献"的拆法 |
| "向量基线 → +BM25 RRF 融合" | ✅（代码）/🟡（对称粒度中间变体数字） | src/retrievers/hybrid_retriever.py；评测/03 步骤② | RRF 融合循环（内嵌如下）；对称粒度 BM25 需新建索引 |
| "→ +粒度不对称" | 🟡 | src/indexer.py build_bm25_index；评测/03 步骤③ | issue 粒度语料（内嵌如下）；贡献值待测 |
| "→ +Cross-Encoder 精排" | ✅（代码）/🟡（该步增量数字） | src/reranker.py | 动态截断精排已实现 |

依据代码内嵌（面试可能被要求"指给我看"）：

```python
# ① 评测集可达性过滤（eval/golden_set_builder.py）
if target_id not in indexed_ids:          # 不可达：检索永远不可能命中
    stats["dropped_unreachable_targets"] += 1; continue

# ② RRF 融合（src/retrievers/hybrid_retriever.py）
for rank, doc in enumerate(vector_results, start=1):
    rrf_scores[doc["id"]] += 1.0 / (RRF_K + rank)      # RRF_K = 60
for rank, doc in enumerate(bm25_results, start=1):
    rrf_scores[doc["id"]] += 1.0 / (RRF_K + rank)

# ③ 粒度不对称的"不对称"一侧：BM25 用整条 issue 建语料（src/indexer.py）
bm25_corpus = [tokenize(f"{issue['title']} {issue['body']}") for issue in all_issues]
# （对称对照组的 chunk 粒度索引按 评测/05-实现规格 suite/chunk_bm25.py 新建）
```

### 2.3 占位符取值表

简历原文共 5 个空位；其中 2 个起点已实测，**真正待跑的是 3 个**：

| 空位 | 含义 | 状态 | 命令 | 取值字段 |
| --- | --- | --- | --- | --- |
| P1 Recall@10 起点 | 向量基线 | ✅ 已有：**0.3965** | （已跑）`python -m eval.run_eval --limit 200 --configs vector_only` | `eval_results/vector_only.json` → `recall@10` |
| P2 Recall@10 终点 | 混合+精排 | 🟡 待跑 | `python -m eval.run_eval --limit 200 --configs hybrid hybrid_rerank` | `eval_results/hybrid_rerank.json` → `recall@10` |
| P3 nDCG@10 起点 | 向量基线 | ✅ 已有：**0.2886** | 同 P1 | `vector_only.json` → `ndcg@10` |
| P4 nDCG@10 终点 | 混合+精排 | 🟡 待跑 | 同 P2 | `hybrid_rerank.json` → `ndcg@10` |
| P5 粒度贡献 +pp | hyb_asym − hyb_sym 的 recall@10 差 | 🟡 待跑（先实现 chunk BM25） | 见 评测/07-待跑清单 步骤 C | 两变体结果的 `recall@10` 之差；**简历需注明 pp 挂的是 Recall@10** |

⚠️ 两条纪律：① P2/P4/P5 填入前，这条 bullet 不投出去；② 修正评测后基线被
抬高（34% 无解题已剔除），P2−P1 的幅度大概率小于旧口径的 +21pp——这是尺子
变准了，不是系统变差了（详见 07 文档末尾说明）。

## 3. 简历未写、但已在评测体系内的内容（口头弹药）

以下从简历拿掉但必须能讲（问答稿在 评测/06 新增节）：

| 内容 | 分级 | 一句话版本 |
| --- | --- | --- |
| 评测集三处失效修正 | ✅ | 34% 不可达剔除、自命中剔除、并查集 family 隔离 |
| nDCG IDCG bug | ✅ | v1 从已检索 gains 算理想序，命中 1 条即满分，退化成 Recall 的另一种写法 |
| 随机性控制 | ✅ 方案 | 检索链路无随机源单次评估；LLM 环节 t=0×3 次报均值±标准差；HNSW 构建随机→重建即断代 |
| Precision@5 / MRR | ✅ | 仍在体系内：P@5 受"单答案上限 0.2"约束需带背景解读；MRR 看首个命中位置 |
