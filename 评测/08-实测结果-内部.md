# 08 实测结果（内部）

> **内部体检数据，未经复核，不得回填简历或 README。**
> 体检日期：2026-07-27。执行范围：评测/07 的步骤 A、B；C1/C2 因涉及写代码
> 本轮跳过（红线：不修任何代码）；重试净收益/阈值扫描因 DeepSeek 余额为 0
> （HTTP 402）阻塞；步骤 D 回填按红线禁止。

## 0. 资产版本锁定（评测/00 合规）

| 资产 | 大小 | sha256 前 16 位 |
| --- | --- | --- |
| bm25.pkl | 106,104,662 B | 13a09c9f5105800e |
| docstore.pkl | 72,534,443 B | 6ada1136bb279a8e |
| eval/data/golden_set.json | 13,889,365 B | 789fa58e306cb525 |
| chroma_db_en/chroma.sqlite3 | 1,262,731,264 B | （未算 1.2GB 文件 hash，以文件大小+目录 mtime 代替；期间未重建） |

golden set build_stats（文件内嵌，seed=42，family 划分）：input 106,656 /
raw duplicate 13,104 / 无可达目标剔除 4,505 / 自引用剔除 4 / 不可达目标剔除
6,114 / 有效 8,599 / family 3,333 / eval 6,873 / test 1,726。

采样：test set 固定 seed=42 抽 200 条（与既有 vector/bm25 结果同一批 query）。
温度设置：本轮无 LLM 环节（检索层确定性评测，各配置跑 1 次，依据 评测/00）。

## 1. 步骤 A：前置检查

| 项 | 命令 | 结果 | 用时 |
| --- | --- | --- | --- |
| 单元测试 | `python3 -m pytest tests/ -q` | 32 passed | 3.8s |
| 资产在位 | `ls chroma_db_en bm25.pkl docstore.pkl eval/data/golden_set.json` | 全部在位 | — |

异常现象：无。

## 2. 步骤 B：hybrid / hybrid_rerank（200 条，自命中剔除）

命令：`HF_HUB_OFFLINE=1 python3 -m eval.run_eval --limit 200 --configs hybrid hybrid_rerank`

| 配置 | recall@5 | recall@10 | recall@30 | precision@5 | MRR | nDCG@10 | P50 | P95 | 用时 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| hybrid | 待运行结束回填 | | | | | | | | |
| hybrid_rerank | 待运行结束回填 | | | | | | | | |

（运行中，本节由后台任务完成后回填。）

参照基线（同评测集同 seed 同 200 条，此前已实测）：

| 配置 | recall@5 | recall@10 | recall@30 | precision@5 | MRR | nDCG@10 | P50 | P95 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| vector_only | 0.3338 | 0.3965 | 0.4835 | 0.074 | 0.2709 | 0.2886 | 0.022s | 0.035s |
| bm25_only | 0.2730 | 0.3337 | 0.4305 | 0.062 | 0.2492 | 0.2589 | 2.901s | 49.016s |

## 3. 本轮未执行项（原因记录）

| 项 | 状态 | 原因/证据 |
| --- | --- | --- |
| C1 chunk 粒度 BM25 索引 | ⛔ 跳过 | 需新写代码（suite/chunk_bm25.py），与本轮"不修任何代码"红线冲突；已约定由用户亲手实现 |
| C2 粒度对称 vs 不对称 | ⛔ 跳过 | 依赖 C1 |
| 重试净收益（retry0 vs retry2） | ⛔ 阻塞 | 需 LLM；DeepSeek 账户 402 Insufficient Balance（2026-07-26 实测，key 尾号 2ac6 认证 200 但余额 0） |
| 阈值扫描 0.5-0.8 | ⛔ 阻塞 | 同上 |
| 步骤 D 数字回填 | ⛔ 红线 | 本轮结果不回填任何对外材料 |

## 4. golden set 分布体检（只读分析，2026-07-27）

按 project 的 eval/test 划分比例（family 级贪心装箱的实际效果）：

| project | eval | test | test 占比 |
| --- | --- | --- | --- |
| firefox | 3,186 | 797 | 20.0% |
| thunderbird | 2,372 | 595 | 20.1% |
| vscode | 442 | 111 | 20.1% |
| spark | 396 | 100 | 20.2% |
| cassandra | 232 | 60 | 20.5% |
| hadoop | 100 | 26 | 20.6% |
| hbase | 85 | 22 | 20.6% |
| seamonkey | 60 | 15 | 20.0% |

test set 答案数分布：1 个答案 1,566 条（90.7%）；2 个 94；3 个 29；
长尾到 30 个答案（各 1-13 条），最大 family 的 query 有 30 个 ground truth。

test set 长度分桶：short(<200 字符) 134 条 / mid 1,306 / long(>1500) 286。

**200 条抽样（seed=42）的 project 构成**：firefox 93 + thunderbird 59 =
**76% 为 Mozilla 系**；spark 14、vscode 12、cassandra 12、hbase 6、
seamonkey 2、hadoop 2。

## 5. 运行异常与观察记录

- run_eval 的 summary.json 每次运行会被**整体覆盖**为本次 configs + _meta，
  本轮跑完后 summary 里将只剩 hybrid/hybrid_rerank（vector/bm25 的单配置
  JSON 仍在，但 summary 不再汇总四配置）。工具行为记录，本轮不修。
- run_eval **只落聚合指标，不落逐 query 明细**——无法事后算抽样置信区间、
  无法做逐条 badcase 归因（05-实现规格 的 QueryRecord 设计还没实现）。
- （运行结束后继续补充。）
