# 05b 项目故事线（下）：故事 3-5 + 使用建议

## 📌 项目速览（每个文档都重复，便于单独阅读）

Issue 查重系统：新 issue 提交后，在 106,714 条历史 issue（GitBugs 数据集，8 个开源项目）里找重复，输出 duplicate / similar / new 三分类 + 置信度 + 相关 issue 编号 + 判断理由。在线流程是 LangGraph 四节点：LLM 改写 query → 双路混合检索（BM25 + 向量各 Top-30，双层 RRF 融合，docstore 补齐正文）→ Cross-Encoder 精排 Top-5 → LLM 决策 + 程序级校验；置信度 < 0.7 且未超 2 次重试时带诊断回环重写。技术栈：Python、LangGraph、DeepSeek、ChromaDB（all-MiniLM-L6-v2，384 维）、rank-bm25、bge-reranker-base、FastAPI。

> 上篇（05a）：两分钟逐字稿 + 故事 1（评测集 34% 无解）+ 故事 2（docstore 补证据）。

## 故事 3：LLM 编造 issue 编号——白名单校验与降级重试

- **S**：Decision 节点偶发返回候选列表里不存在的 issue id。对查重系统这是致命的
  ——维护者按编号点过去发现不存在，对系统的信任直接归零。prompt 里其实早就写了
  "只能从候选列表选"，但提示词约束是概率性的，挡不住所有情况。
- **T**：把"返回的每个相关 id 都真实存在于本轮候选"变成程序保证的硬性质，而不是
  祈祷模型听话。
- **A**：在 Decision 节点加了一层程序级校验：decision 必须命中三个枚举，否则按
  解析失败降级；confidence 强转并截断到 [0,1]；related_issues 逐个对照本轮精排
  候选的 id 白名单，不在的过滤并记日志。最微妙的是决策语义：如果一个 duplicate
  判断过滤后一个合法 id 都不剩，这个结论就没有任何可核查证据——我选择把它降级为
  new 并把置信度压到 0.4 以下，这样低置信度会在重试预算内自动触发新一轮检索，
  系统"自己再试一次"，而不是带着幻觉结论结束。
- **R**：幻觉 id 从"偶发泄漏给用户"变成"结构上不可能返回"；7 个针对性单元测试
  覆盖枚举、截断、白名单、降级四条路径。

<details>
<summary>点开看核心代码（src/agent/nodes.py decision_node 校验段）</summary>

```python
candidate_ids = {str(doc.get("id", "")) for doc in state["reranked_docs"]}
related_issues = [str(i) for i in related_issues if str(i) in candidate_ids]  # 白名单

if decision in ("duplicate", "similar") and not related_issues:
    decision = "new"                       # 无可核查证据 -> 安全降级
    confidence = min(confidence, 0.4)      # 0.4 < 0.7 -> 重试预算内自动再来一轮
    reasoning = f"{reasoning}（原判断引用的 issue ID 未通过候选白名单校验，已降级）"
```
</details>

## 故事 4：改写是把双刃剑——双路召回兜住上游单点故障

- **S**：Query 改写让检索式更技术化，但复盘 badcase 时发现一类失败：LLM 改写时
  把原文里的关键错误码删掉了，或者术语改写发生漂移，导致真正的 duplicate 在召回
  层就彻底丢了——上游一步错，下游 Rerank 和 Decision 再强都无力回天。
- **T**：在不放弃改写收益的前提下，消除"单次 LLM 输出决定全链路生死"的结构风险。
- **A**：把检索改成双路：改写 query 和原始文本各跑一遍完整的混合检索，再用第二层
  RRF 融合两路排名。几个细节：两路按 casefold 去重，改写失败回退原文时只跑一路，
  不白花一倍时间；外层融合同分时利用稳定排序让改写主路排前；同一 issue 在两路的
  正文完整度不同时保留更完整的版本。同时我写了一个对照 benchmark，固定改写结果
  和标注，量化双路相对单路的 rescue 率和 regression 率——因为它需要人工确认的
  案例，我没有编数据，目前是"框架就绪待标注"状态，这点我如实说。
- **R**：结构上，改写幻觉从"全链路失败"降级为"损失一路信号"；行为上有单元测试
  锁住去重、融合和证据保留逻辑。待 verified 案例标注后可以给出量化结论。

<details>
<summary>点开看核心代码（src/retrievers/hybrid_retriever.py search_queries 节选）</summary>

```python
# 两路按 casefold 去重：改写失败回退原文时两路相同，只检索一次
unique_queries, seen = [], set()
for query in queries:                       # queries = [rewritten_query, raw_issue]
    normalized = query.strip()
    if not normalized or normalized.casefold() in seen:
        continue
    seen.add(normalized.casefold())
    unique_queries.append(normalized)

# 外层 RRF：同一 issue 被两路都召回则加分两次，排名自然提高
for docs in ranked_lists:
    for rank, doc in enumerate(docs, start=1):
        rrf_scores[doc["id"]] += 1.0 / (RRF_K + rank)
```
</details>

## 故事 5：一次 401 排查——配置加载的隐蔽优先级

- **S**：v2 联调时所有 LLM 调用突然 401 认证失败。检查 .env 文件，key 是对的；
  用 curl 直接测这个 key，也是通的。
- **T**：找出"配置明明正确却认证失败"的原因。
- **A**：细看报错里的 key 尾号，和 .env 里的对不上——顺着查发现 shell 环境变量里
  残留着一个早已失效的 DEEPSEEK_API_KEY，而 python-dotenv 的 load_dotenv() 默认
  **不覆盖**已存在的环境变量，所以项目一直在用那个死 key。修复很小：
  `load_dotenv(override=True)`，让项目 .env 显式优先，并把这个坑写进代码注释和
  CLAUDE.md 的踩坑记录。排查过程中还发现一个更严重的问题：v1 的 .env.example
  里提交的是一个真实有效的 key，已经进了 git 历史——立即把示例文件改成占位符、
  真实 key 移入 gitignored 的 .env，并标记该 key 需要作废轮换。
- **R**：401 根因清除；顺带堵住一个真实的凭据泄漏。教训有两条：报错信息里的
  每个字段都值得对一遍（key 尾号直接指向了真相）；示例配置文件里永远只放占位符。

<details>
<summary>点开看核心代码（config.py 开头）</summary>

```python
# override=True：项目 .env 优先于 shell 继承的同名环境变量。
# 实际踩坑：shell 里残留失效的 DEEPSEEK_API_KEY 时，默认行为会静默用旧 key 导致 401。
load_dotenv(override=True)
```
</details>

## 讲故事的使用建议

- 被问"最大的挑战"→ 讲故事 1（评估重建），它有数字（34%）、有方法论（先验尺子）、
  有可复现产物，含金量最高。
- 被问"debug 能力"→ 讲故事 5（401），短、真实、有反转。
- 被问"工程严谨性"→ 讲故事 3（白名单），落点是"把概率性约束变成结构性保证"。
- 被问"系统设计"→ 讲故事 2（docstore）或 4（双路），落点都是"识别单点故障并兜底"。
- 每个故事结尾主动补一句局限（故事 4 的"待标注"、故事 1 的"分类层还没标注集"），
  诚实是最强的可信度信号。

### ✅ 自测（故事 3-5）

1. 故事 3 里"降级到 confidence ≤ 0.4"的设计巧思是什么？
2. 故事 4 为什么强调"串行 fallback 兜不住"？双路的代价是什么、怎么被缓解？
3. 故事 5 的两条通用教训是什么？分别对应什么工程习惯？

---

## 自测答案

<details>
<summary>答案</summary>

1. 0.4 低于重试阈值 0.7：降级不是简单吞掉错误，而是让 should_retry 在预算内
   自动触发新一轮检索——给系统一次"自我纠错"的机会；预算耗尽时才以保守结论收尾。
2. 串行 fallback 只能捕获"改写格式失败"；"格式成功但语义漂移/删信号"无法检测，
   只有原文路常驻才能兜住。代价是检索耗时接近翻倍；缓解：两路相同自动去重、
   （量化收益/回退由 dual_query benchmark 负责，待 verified 案例）。
3. ① 报错的每个字段都要核对（key 尾号不一致直接指向 shell 残留变量）——对应
   "读完整报错再动手"的习惯；② 示例配置永远只放占位符——对应"凭据不进 git"
   的习惯（一旦提交，历史里永远有）。
</details>
